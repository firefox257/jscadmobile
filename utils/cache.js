

$.cache = function(project, comp)
{

	/*
	directory structur
	/project(directory)/comp(directory)
	Files in directory
	code.js
	cache.js // is an object array of each step call.
	stepcache_(id 0 to N).json
	stepcache_(id 0 to N).json

	*/
 //to do to load code and cache.json into objecgt.

 //return new object
	return
	{

	};








};
